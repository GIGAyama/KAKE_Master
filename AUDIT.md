# GIGA Standard v5 監査：九九カード（KAKE_Master）

- **アーキテクチャ**：A型（単一HTML完結・ビルド不要・依存ライブラリなし）
- **測定日**：2026-08-04
- **測定環境**：Chromium（Playwright）/ 320×568・375×667・1366×768 の3幅 × 6画面 = 18通り
- **走査量**：文字要素 2,835 個 / ボタン 1,245 個
- **再現手順**：`npm run check` / `npm run measure:ui` / `npm run measure:pwa` / `npm run measure:icons` / `npm run smoke`

> **「未計測」は ✅ ではない。** 測っていないものは「未計測」と書いてある。
> 実測値はすべて上記のコマンドで再現できる。

---

## 総括（前 → 後）

| 項目 | 前 | 後 |
|---|---:|---:|
| コントラスト基準未満 | **137 件**（32パターン） | **0 件** |
| タップ領域 44px 未満 | **9 件** | **0 件** |
| 横スクロール（320px 幅） | 0 件 | 0 件 |
| CSP 違反 | —（CSPなし） | **0 件** |
| JS エラー（フォント読込失敗を除く） | 0 件 | **0 件** |
| PWA 挙動テスト | **5/10 合格** | **10/10 合格** |
| アイコン合計 | **222.0 KB** | **75.3 KB** |
| 初回に必要な JS | 73.1 KB | **74.0 KB** |
| 初回に必要な資産（フォント除く） | 約 285 KB | **139.3 KB** |
| CDN から取る実行コード | **0 バイト** | **0 バイト** |

---

## A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | 新規作成（MIT / Copyright (c) 2026 GIGAyama） |
| A2 | .gitignore | ✅ | 新規作成（node_modules / dist / .clasp.json / .env / .DS_Store） |
| A3 | dependabot.yml | ✅ | 新規作成（npm・github-actions とも monthly） |
| A4 | README.md / MANUAL.md / AUDIT.md | ✅ | MANUAL.md・AUDIT.md を新規作成、README に構成と実測手順を追記 |
| A5 | CI が pull_request で動く | ✅ | `.github/workflows/ci.yml` を新規作成。`push: [main]` と `pull_request` の両方 |

## B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（**入れたうえで動作確認済み**） | ✅ | `<meta>` で投入。**投入後に実ブラウザで全モードを操作し、CSP違反 0 件・JSエラー 0 件を確認**（`npm run smoke`） |
| B2 | 秘密情報・IDの直書きなし | ✅ | APIキー・秘密鍵のパターン検出 0 件。そもそも通信先が無い |
| B3 | OAuthスコープ最小 | — | 対象外（GAS を使っていない） |
| B4 | postMessage の宛先が `*` でない | ✅ | `postMessage` は Service Worker への `{type:'SKIP_WAITING'}` のみ。クロスオリジン送信なし |
| B5 | サーバー側5段ガード | — | 対象外（サーバーが無い。記録は端末内 localStorage のみ） |
| B6 | CDN から取る実行コードが 0 | ✅ | **0 バイト**。`@babel/standalone` / Tailwind CDN / CDN の React ともに無し（改修前から無し） |
| B7 | 残る外部資産に SRI と版の固定 | — | 対象外。残る外部資産は Google Fonts のみで、これは**実行コードではなく見た目だけ**の依存（§2-7）。届かなくても端末側フォントに落ちるだけで動く。SRI は Google Fonts の返す CSS が UA ごとに変わるため付けられない |

### CSP に入れた内容と、その理由

```
default-src 'self'; script-src 'self';
style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;
font-src  'self' https://fonts.gstatic.com;
img-src   'self' data: blob:; connect-src 'self';
worker-src 'self'; manifest-src 'self';
object-src 'none'; base-uri 'self'; form-action 'self';
```

- `script-src` に **`'unsafe-inline'` は足していない。** インラインの `<script>` と `onclick=` は
  もともと1つも無く、`beforeinstallprompt` の捕捉も新設の外部ファイル `install-hook.js` に置いた。
- `style-src` の `'unsafe-inline'` は、SVGスプライトの `style="display:none"` などの
  style 属性のために必要。
- **`frame-ancestors` は書いていない。** `<meta>` では無視され、読み込みのたびに
  コンソールへ警告が出るだけになるため。独自ドメインや CDN を挟むときに
  HTTP ヘッダーで設定する旨を `index.html` のコメントに残した。

## C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally | — | 対象外（GAS を使っていない） |
| C2 | 自動復旧 | ✅ | `storage.js` が localStorage の読み書きを try/catch で包み、プライベートモードや保存不可でも続行する（改修前から） |
| C3 | pagehide で記録確定 | ✅ | `studySession.js:257` で `pagehide` を購読済み（改修前から） |
| C4 | 通信失敗時のリトライと明示 | — | 対象外（通信しない）。圏外時は Service Worker がキャッシュ、無ければ `offline.html` を出す |
| C5 | localStorage.clear() を使っていない | ✅ | 0 件。リセットは自アプリ接頭辞（`kuku-card-v1`）だけを消し、アプリ共通の `study.records.v1` は残す（改修前から） |

## D. 表示（Part I §2）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| D1 | viewport に viewport-fit=cover | ✅ | 改修前から有り（GAS でないので `code.gs` は対象外） |
| D2 | 100dvh を使用（100vh 単独でない） | ✅ | 「`100vh` を書いてから `100dvh` で上書き」という書き方を、**`dvh` を先に書き `@supports not` で受ける形**に変更（4か所） |
| D3 | safe-area-inset を適用 | ✅ | 上下のみ → **左右も追加**（横向き時のノッチ側の欠けを防ぐ）。適用 4方向 |
| D4 | clamp() による fluid type | ✅ | 11 か所。本文にも `clamp(15px, 1.6vw + 10px, 18px)` と `line-height: 1.8` を追加 |
| D5 | Canvas に devicePixelRatio 補正（上限2） | ✅ | **改修前は補正なし**。紙ふぶきの Canvas に `Math.min(devicePixelRatio, 2)` と `setTransform` を追加 |
| D6 | 320px 幅で横スクロールが出ない | ✅ | **0 件**（3幅 × 6画面で実測）。81マスのヒートマップは枠内スクロールにしてページ全体には出さない |
| D7 | 画像に width/height、150KB以下 | ✅ | `<img>` は 0 個（図はすべてインラインSVG）。PNG は全てアイコンで、最大 28.9KB |
| D8 | **コントラスト 4.5:1 以上（実ブラウザで全画面を走査）** | ✅ | **137 件 → 0 件**（下に内訳） |
| D9 | **タップ領域 44px 以上（::after 込みで実測）** | ✅ | **9 件 → 0 件**（下に内訳） |
| D10 | prefers-reduced-motion 対応（.01ms） | ✅ | **改修前は未対応**。`.01ms` で追加（`0` にすると `fill-mode: forwards` が壊れ、`fadeIn` している画面が消えるため） |
| D11 | forced-colors 対応 | ✅ | **改修前は未対応**。主要なボタン19種に `border: 2px solid ButtonText` を追加 |
| D12 | 提示モード | ❌ | **未実装。人間の判断を仰ぐ**（下の「人間に決めてほしいこと」参照） |
| D13 | 印刷CSS | ✅ | きろく画面向けに追加（A4たて／ボタン・帯を除外／カード分割を防ぐ）。手順は MANUAL.md 5章 |
| D14 | 拡大を禁止していない | ✅ | **改修前は `user-scalable=no` が付いていた** → 削除。誤ズームは `touch-action: manipulation` と `.canvas-area { touch-action: none }` で防ぐ |

### D8 コントラストの内訳（32パターンすべて）

いちばん低かったものから。**案内文・空状態・補足が下位を占めている**——
つまり「いちばん困っている人に向けた文が、いちばん読みにくい」形になっていた。

| 比 | 要る比 | どこ | 何の文か | 直し方 |
|---:|---:|---|---|---|
| 1.34 | 4.5 | `.kface-back .kfoot` | カード裏の「うら」 | 面を2段濃く＋文字を amber-50 |
| 1.36 | 4.5 | `.hm-0` / `.cal-num` | 未練習のマスの数字・日付 | slate-300 → `--ink-sub` |
| 1.48 | 4.5 | `.kfoot` / `.cal-num` | カード表の「おもて」 | slate-300 → `--ink-sub` |
| 1.48 | 4.5 | `.kface-back .ktag` | カード裏の「こたえ」ラベル | 半透明の白い帯 → 黒 22% の帯 |
| 1.50 | 4.5 | `.kyomi` | **となえかた「にさんが ろく」** | 同上＋面を2段濃く |
| 1.67 | 3 | `.btn-start` | **「はじめる!」** | 面 amber-400/500 → **amber-700/800** |
| 1.67 | 4.5 | `.start-count` | 「(9まいの カード)」 | 同上 |
| 1.67 | 3 | `.kanswer` | **こたえの数字（いちばん大事）** | 面を2段濃く |
| 2.15 | 4.5 | `.stage-btn.on` | 選択中の段 | 面 amber-500 → amber-700 |
| 2.15 | 3 | `.key-ok` | キーパッドの OK | 同上 |
| 2.41 | 4.5 | `.play-hint` | **「カードをタップでめくる…」（操作の案内）** | slate-400 → `--ink-sub` |
| 2.56 | 4.5 | `.mode-desc` ほか10種 | モードの説明・曜日・段ラベル・補足 | slate-400 → `--ink-sub` |
| 2.86 | 4.5 | `.ctl-flip` | 「めくる」 | amber-600 → amber-800 |
| 3.30 | 4.5 | `.ctl-good` | 「できた!」 | green-600 → green-700 |
| 3.67 | 4.5 | `.ctl-again` / `.btn-danger` | 「もういちど」「きろくをぜんぶけす」 | rose-500 → rose-600 |
| 4.27 | 4.5 | `.pp-name` | プレイヤー名 | slate-500 → `--ink-sub` |
| 4.34 | 4.5 | `.stage-btn` / `.seg-btn` | 未選択の段・順番 | slate-500 → `--ink-sub` |
| 4.46 | 4.5 | `.home-today` / `.progress-text` | **「きょうは まだ れんしゅうしていないよ」（空状態）** | slate-500 → `--ink-sub` |

**色相は変えていない。** 面か文字を1〜2段濃くしただけで、配色の印象はほぼ変わらない。
文字用の色は変数2つに集約した（`--ink: #1e293b` / `--ink-sub: #475569`）。
`--ink-sub` は白地でも淡い地（`#fff7e6` / `#f1f5f9` / `#fef3c7`）でも 4.5:1 を満たす。

> **一括置換はしていない。** `slate-400 → 500` のような一括置換は
> 「濃い面の上の薄い文字」を巻き添えにして逆に読めなくする。
> 88 か所を行ごとに指定して当て、**当てたあとに測り直して 0 件を確認した。**

### D9 タップ領域の内訳（9件すべて）

**ボタン自体は大きくしていない。** 詰めて組んであるツールバーで `min-height` を当てると
折り返しが起きて別の破綻を生むので、`.tap-44::after` で当たり判定だけを広げた。

| 前 | 要素 | 何のボタンか |
|---|---|---|
| 22×34 | `.hm-cell` | 81マスのヒートマップ |
| 96×29 / 80×29 | `.btn-mini` | 「ぜんぶえらぶ」「1つにする」 |
| 56×32 | `.toggle` | ふりがなの切り替え |
| 81×34 | `.btn-back` | 「もどる」 |
| 144×36 | `.brand` | ヘッダーのアプリ名 |
| 41×41 | `.kspeak` | カードの読み上げ |
| 42×42 | `.btn-close` / `.btn-sound` | 「やめる」「おと」 |

**ヒートマップだけは疑似要素で解けなかった。** 320px 幅では1マス 22px しかなく、
44px の当たり判定を作ると隣のマスと重なって**別のマスを押してしまう**。
そこでマスを `minmax(44px, 1fr)` にし、**その枠の中だけ横スクロール**させた。
ページ全体の横スクロールは 0 件のまま（実測）。

## E. PWA（Part I §3）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| E1 | manifest の id/scope/start_url がリポジトリ名絶対パス | ✅ | `"./"`・`id` 無し → **`/KAKE_Master/`**（3つとも）。根拠は下記 |
| E2 | アイコン4種 + 透明を含まない apple-touch-icon | ✅ | maskable-192 を**新規追加**して4種そろえた。apple-touch-icon の**透明画素 0.00%**（実測） |
| E3 | beforeinstallprompt を head 最上部で捕捉（外部ファイル） | ✅ | **改修前は `js/app.js`（`type="module"` なので解析後に実行）で待っていた** → `install-hook.js` を新設し `<head>` で同期読み込み |
| E4 | インストールボタン（案内できるときだけ表示） | ✅ | `display-mode: standalone` のときと、合図が来ていないときは出さない |
| E5 | sw.js が自アプリ接頭辞のキャッシュのみ削除（**実測**） | ✅ | 別アプリ名のキャッシュを置いて版を上げ、**残っていることを実測**。自アプリの古いキャッシュは消えることも実測 |
| E6 | sw.js が localStorage に触れていない | ✅ | 0 件 |
| E7 | 更新通知（押すまで切り替わらないことを実測） | ✅ | **改修前は `install` の中で `skipWaiting()` していた** → 削除し、更新の帯＋`SKIP_WAITING` メッセージ方式に。**帯が出る／3秒放置しても読み込み直さない／押したら読み込み直す**の3点を実測 |
| E8 | 初回訪問で勝手にリロードしない（画面遷移1回） | ✅ | **文書の読み込み 1 回**（実測） |
| E9 | Service Worker が実際に登録されている | ✅ | `getRegistration()` で `scope=/KAKE_Master/`・active を確認。登録に `readyState === 'complete'` の分岐を追加 |
| E10 | offline.html（外部資産・JS に頼らない） | ✅ | **新規作成**。CSS は自前・JS 0行・再読み込みは `<a href>`。本体キャッシュを消して**サーバーを止めた状態で出ることを実測** |
| E11 | APP_VERSION を今回のリリース値に更新した | ✅ | `1.2.0 → 1.3.0`（`sw.js` の `VERSION`・`js/studySession.js` の `APP_VERSION`・`package.json`・`quality.config.json` の4か所） |
| E12 | maskable のセーフゾーン外の中身 0.2% 以下 | ✅ | maskable-512 **0.00%** / maskable-192 **0.00%**（画素を数えて実測。下地と中身は色で区別） |
| E13 | iOS の「ホーム画面に追加」手順を MANUAL に記載 | ✅ | MANUAL.md 1章に手順と、ITP で記録が消える件の注意を記載 |

### E1 の根拠（`id` 変更は停止条件ではない）

`/KAKE_Master/manifest.webmanifest` にある `"./"` の解決結果は `/KAKE_Master/` である。
**表記を絶対パスに変えても解決結果は同一**なので、すでにインストール済みの端末で
別アプリ扱いにはならない。
`id` はもともと**省略されていた**が、省略時の既定値は `start_url`（＝ `/KAKE_Master/`）なので、
これも同一性を変えない。したがって §停止条件の「`id` 変更で既存インストールが別扱いになる」には該当しない。

明示する理由は、`gigayama.github.io` を数十個のアプリが**同一オリジンで共有**しているため。
`id` を省略したままだと、URLを少し直しただけで別アプリ扱いになったり、
似た構成の別アプリと取り違えられて「開いたら違うアプリが立ち上がる」事故が起きる。

### PWA 挙動テストの結果（`npm run measure:pwa`）

```
✅ E9  Service Worker が登録されている — scope=…/KAKE_Master/
✅ E8  初回訪問で勝手にリロードしない — 文書の読み込み 1 回
✅ E10a 圏外で起動する — ホーム画面が出た
✅ E10b 本体が無いとき offline.html が出る — オフライン用の画面が出た
✅ E7  新版は押すまで待機する（3秒放置） — waiting のまま留まった
✅ E5  他アプリのキャッシュが残っている
✅ E5b 自アプリの古いキャッシュは消える
✅ E7b 更新の帯が出る
✅ E7c 押すまで読み込み直さない — 文書の読み込み 1 回
✅ E7d 押したら読み込み直す — 文書の読み込み 2 回
合計: 10/10 合格   （改修前: 5/10）
```

## F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | ✅ | `<img>` は 0 個。アイコンのみのボタンに `aria-label`、中の SVG に `aria-hidden`。**`aria-live` をこたえあわせ（`#quiz-feedback`）とマスの説明（`#heat-detail`）に追加**。トーストは `role="status"`（改修前から） |
| F2 | モーダルに role="dialog"・Esc で閉じる | — | 対象外（モーダルが無い）。ただし Esc は既存の「もどる」処理に繋がっている |
| F3 | キーボードのみで全機能に到達 | ⚠️ **一部未計測** | ← / → / スペース / Enter / 数字 / Backspace / Esc の対応は改修前から有り。`:focus-visible` の輪郭を追加。**Tab 順が視覚順と一致するかは未計測** |
| F4 | rt の色を決め打ちしていない | — | 対象外（`<ruby>` / `<rt>` を使っていない）。ふりがなは `.kyomi` として表示しており、その比は D8 で 1.50 → 基準達成 |
| F5 | 初回JS 300KB以下 | ✅ | **74.0 KB**（gzip 前） |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | 最大は `js/app.js` の 1,116行 / 39.9 KB |
| F7 | 総アセット（初回）1MB 以下 | ✅ | **139.3 KB**（Web フォントを除く。フォントは届かなくても動く） |
| F8 | 見出しの階層 | ✅ | **改修前は `<h1>` が無く `<h2>` から始まっていた** → 読み上げ用の `<h1>` を追加 |

## G. 学習ログ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| G1 | study.v1 準拠・個人情報を持たない | ✅ | `study.records.v1` に保存。実際に1周プレイして書き出された記録を検査し、メール・氏名・出席番号らしき値が無いことを確認（`npm run smoke`）。外部送信なし |
| G2 | 中断記録・5分ルール | ✅ | `studySession.js` に `status:"aborted"` と 5分の離席判定が実装済み（改修前から） |

---

## 品質ゲート

```
npm run check              → 53/53 合格
npm run check:self-test    → 25/25 合格（わざと壊して落ちることを確認）
npm test                   → 7/7 合格
```

**「0件でした」だけでは、検査が動いているのか何も見ていないのか区別できない。**
`--self-test` は各検査が見ているファイルを1つずつわざと壊し、
**その検査がちゃんと落ちること**を確かめる。

### この確認で見つかった、検査そのものの不具合（4件）

| 検査 | 何が起きていたか |
|---|---|
| `B_CSP_NO_UNSAFE_INLINE_SCRIPT` | 「script-src に `'unsafe-inline'` は足さない」という**注意書きに反応**していた |
| `B_CSP_NO_FRAME_ANCESTORS` | 「frame-ancestors はここに書いても無視される」という注意書きに反応 |
| `B_NO_INLINE_SCRIPT` | 注意書きの中の `<script>` の字面に反応 |
| `D_NO_BARE_100VH` | 「100vh ははみ出すので dvh を使う」という注意書きに反応 |

いずれも **「正しく直して、その理由をコメントに書いたリポジトリほど落ちる」** という、
いちばん困る形だった。判定の前に HTML／CSS のコメントを落とすことで解決した。
（v5 §P4 が JS について挙げている `SW_LOCALSTORAGE` の誤検知と同じ形が、HTML と CSS でも起きる）

---

## 測っていないもの（未計測）

正直に列挙する。**これらは ✅ ではない。**

| 項目 | なぜ測れていないか |
|---|---|
| **本番（`kake-master.giga-school.com`）での動作** | 作業環境からこのホストへ到達できない（プロキシが遮断）。すべて手元の HTTP サーバーをサイトの直下に見立てて測った。**旧構成では `/KAKE_Master/` の下に見立てて測っており、それが下記（2026-08-20）の見逃しの原因になった** |
| **実機の Chromebook / iPad / 電子黒板** | 実機が無い。Chromium のビューポート指定と DPR=2 での測定にとどまる |
| **iOS Safari の挙動全般** | WebKit で測っていない。`beforeinstallprompt` が無いこと・ITP の7日制限・apple-touch-icon の黒枠は、仕様に基づく対処のみ（透明画素 0.00% は実測済み） |
| **Web フォントが届く環境での見え方** | `fonts.googleapis.com` へ到達できないため、**端末側フォント（フォールバック）での見え方だけを測った**。これは学校のフィルタリング下と同じ状態なので、むしろ本番に近い。ただし「Zen Maru Gothic が当たった状態」のコントラストは未計測（字形が変わるだけで色は変わらないため、比は同じはず） |
| **Tab 順が視覚順と一致するか（F3）** | キーボード操作の対応は確認したが、Tab 順そのものを1画面ずつ追ってはいない |
| **印刷の実出力** | 印刷CSS は書いたが、実際にプリンタへ出していない。印刷プレビューでの確認手順は MANUAL.md 5章に記載 |
| **読み上げソフトでの実聴取** | `aria-live` / `aria-label` / `<h1>` は入れたが、NVDA・VoiceOver で実際に聞いてはいない |
| **横断共有の検査の正本** | `scripts/lib/project-quality.mjs`（正本）は他リポジトリにあり取得できなかった。Part I の検査（`scripts/lib/giga-v5-checks.mjs`）のみで構成し、正本が置かれれば自動で合成する形にしてある |

---

## 人間に決めてほしいこと

1. **提示モード（D12）を入れるか。**
   一斉授業で電子黒板に映す使い方があるなら、Part I §2-11 は「必ず用意する」としている。
   ただしこれは**新しい機能の追加**であり、v5 の改修モード（P1）の手順には含まれていない。
   また、このアプリのプレイ画面は縦横の余白をぎりぎりまで使う作りなので、
   `font-size: 150%` を当てるとカードとキーパッドの取り合いになり、レイアウトの作り直しが要る。
   **現状はブラウザの拡大（禁止を解除済み）と全画面で代替できる**ため、
   今回は入れず、判断を仰ぐことにした。

2. ~~**`manifest` の絶対パス化にともなう公開場所の固定。**
   `id`/`scope`/`start_url` を `/KAKE_Master/` に固定した。
   **リポジトリ名を変えたり、独自ドメインの直下（`/`）に置いたりすると動かなくなる。**
   公開場所を変える予定があれば教えてほしい。~~
   → **解決済み。** 独自ドメイン `kake-master.giga-school.com` へ移行した。
   ここで書いたとおりの形で実際に壊れた。経緯は下記の追記（2026-08-20）を見ること。

3. **共用端末での記録の扱い。**
   記録は端末内に1人分しか持てない。1台を複数の児童が使う運用があるなら、
   記録機能の扱い（切る・注意書きを出す・端末ごとに割り当てる）を決める必要がある。
   現状は MANUAL.md 3章に注意書きを載せるにとどめた。

---

## 他のリポジトリにも効く知見

`ROLLOUT.md` にも同じ内容を書いた。

1. **v5 §7-2 のコントラスト測定コードには、アルファの割り戻しの誤りがある。**
   `getImageData` が返す RGB は**アルファを掛ける前**の値なので、`d[0]/a` と割り戻すと
   `rgba(255,255,255,.25)` が `rgb(1016,…)` になる。半透明の帯の上の文字が
   実際よりずっと明るく判定され、**見逃す**。割り戻してはいけない。
2. **背景の合成で「下地は白」と決め打つと、グラデーションの上の文字が「白の上の白（比 1.0）」になる。**
   外側から内側へ、取りうる色を全部たどって合成し、いちばん不利な組み合わせを採る。
3. **「勝手にリロードしていないか」を `framenavigated` で数えてはいけない。**
   `history.pushState` でも飛ぶので、画面遷移に History API を使うアプリは
   正常でも 3 回と出る。数えるべきは**文書が何回読み込み直されたか**。
4. **圏外を Playwright の `context.setOffline` で作ってはいけない。**
   ページ側の通信にしか効かず、**Service Worker の中の `fetch()` はそのまま外へ出ていく。**
   「本体キャッシュを消したのにアプリが出た＝ offline.html が出ない」という
   まぎらわしい結果になる（出ていないのではなく、圏外になっていない）。
   サーバーを本当に止め、さらに `Network.clearBrowserCache` でブラウザのキャッシュも消す。
5. **静的検査は、規則を説明したコメント自身に反応する。**
   HTML と CSS でも起きる（v5 §P4 は JS の例だけ挙げている）。判定前にコメントを落とす。
6. **`::after` でタップ領域を広げる手は、密なグリッドには使えない。**
   隣と重なって別の要素を押してしまう。枠内スクロールに逃がすほうが安全。

---

## 追記：独自ドメイン移行で「インストール」ボタンが消えた（2026-08-20）

**症状** `kake-master.giga-school.com` へ移したあと、ヘッダーの「インストール」ボタンが出ない。

**原因** `manifest.webmanifest` の `icons[].src` だけ、旧構成の絶対パス
`/KAKE_Master/icons/…` が残っていた。`id`/`scope`/`start_url` は `"./"` に直してあったが、
`icons` を見落としていた。独自ドメインではアプリがサイトの直下で配信されるため、
4枚とも `https://kake-master.giga-school.com/KAKE_Master/icons/…` を指し、**すべて 404**。

Chrome は「取得できるアイコンが1枚も無い」とインストール可能と判断しないので、
`beforeinstallprompt` が飛ばない。ボタンは `window.__pwaInstallPrompt` があるときだけ出す作りなので、
**押しても何も起きないボタンではなく、ボタンが出ないまま**になる。
`Page.getAppManifest` の `errors` は空、コンソールにも何も出ない。**静かに壊れる形だった。**

**確かめ方（実測）** サイトの直下に見立てたローカルサーバーで開き、`icons[].src` を解決して取りに行った：

```
NG   404  /KAKE_Master/icons/icon-192.png      ->  http://127.0.0.1:PORT/KAKE_Master/icons/icon-192.png
NG   404  /KAKE_Master/icons/icon-512.png      ->  …
NG   404  /KAKE_Master/icons/maskable-192.png  ->  …
NG   404  /KAKE_Master/icons/maskable-512.png  ->  …
インストールボタン: {"exists":true,"hidden":true,"prompt":false}
```

**直したもの**

| ファイル | 直した内容 |
|---|---|
| `manifest.webmanifest` | `icons[].src` を `./icons/…`（相対）にした |
| `sw.js` / `quality.config.json` / `package.json` / `js/studySession.js` | 版を 1.3.2 に上げた。**上げないと、既存の利用者は `kuku-app-v1.3.1` に入った壊れた manifest を cache-first で読み続ける** |
| `tools/measure-pwa.mjs` | 測る場所を `/KAKE_Master` の下からサイトの直下へ。あわせて **E11「manifest のアイコンが全部取れる」** を追加 |
| `tools/smoke.mjs` | 既定の `--base` を `http://127.0.0.1:8001/KAKE_Master` から `http://127.0.0.1:8001` へ |
| `scripts/lib/giga-v5-checks.mjs` | **`E_ICON_SRC`** を追加。`icons[].src` が配信先の実ファイルを指しているかを見る |

**なぜ検査を素通りしたか** 既存の `E_ICON_*` は `purpose:sizes` の4通りが揃っているかしか見ておらず、
`src` がどこを指すかを見ていなかった。`measure:pwa` も `/KAKE_Master` の下で測っていたため、
本番と配置が違い、404 が再現しなかった。**検査と実測の両方が、同じ理由で見えていなかった。**

**他のリポジトリにも効く知見** 配信場所を変えるときは、`id`/`scope`/`start_url` だけでなく
**`icons[].src` を必ず一緒に見ること**。そして**実測は本番と同じ配置で行うこと**。
サブディレクトリの下で測ると、絶対パスの取りこぼしはひとつも見つからない。
